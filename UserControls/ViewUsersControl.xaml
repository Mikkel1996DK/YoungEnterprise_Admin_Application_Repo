<UserControl x:Class="YoungEnterprise_Admin_Application.UserControls.ViewUsersControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:YoungEnterprise_Admin_Application.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="550" d:DesignWidth="800">
    <Grid Background="White">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.5*"/>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="8*"/>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="0.5*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="1.5*"/>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="6*"/>
        </Grid.RowDefinitions>

        <!-- Header Label -->
        <Label Content="Alle Brugere" Grid.Row="0" Grid.Column="2" Style="{StaticResource headerLabel}"/>

        <!-- Search Bar / Search Button -->
        <TextBox x:Name="searchUsersTextBox" Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="2" Margin="12, 20, 12, 4" VerticalContentAlignment="Center" FontFamily="Arial" FontSize="22"/>
        <Button x:Name="searchUsersButton" Content="Søg" Grid.Row="1" Grid.Column="3" Margin="12, 20, 12, 4" FontFamily="Arial" FontSize="26"/>

        <!-- ComboBox/Amount-Of-Users Label -->
        <ComboBox Grid.Column="1" Grid.ColumnSpan="2" Grid.Row="2" Margin="12, 4, 12, 20" FontSize="26" FontFamily="Arial" VerticalAlignment="Center" VerticalContentAlignment="Center">
            <ComboBoxItem Content="Vis Alle" IsSelected="True"/>
            <ComboBoxItem Content="Skoler"/>
            <ComboBoxItem Content="Dommere"/>
        </ComboBox>
        <Label x:Name="amountOfUsers" Content="0 Brugere" Grid.Column="3" Grid.Row="2" VerticalAlignment="Center" HorizontalAlignment="Center" FontFamily="Arial" FontSize="18" Margin="12, 4, 12, 20"/>

        <!-- DataGrid -->
        <!-- The center-header is kind of wonky, a better solution might be needed! -->
        <!-- The Team doesn't make sense since we changed some stuff, we will need to discuss this part! -->
        <DataGrid Grid.Column="1" Grid.Row="3" Grid.ColumnSpan="3" Margin="12, 0, 12, 12" AutoGenerateColumns="False" FontFamily="Arial" FontSize="20">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Username" Width="2*" IsReadOnly="True">
                    <DataGridTextColumn.HeaderStyle>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="HorizontalContentAlignment" Value="Center"/>
                        </Style>
                    </DataGridTextColumn.HeaderStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Type" Width="1*" IsReadOnly="True">
                    <DataGridTextColumn.HeaderStyle>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="HorizontalContentAlignment" Value="Center"/>
                        </Style>
                    </DataGridTextColumn.HeaderStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Team" Width="1*" IsReadOnly="True">
                    <DataGridTextColumn.HeaderStyle>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="HorizontalContentAlignment" Value="Center"/>
                        </Style>
                    </DataGridTextColumn.HeaderStyle>
                </DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>

    </Grid>
</UserControl>
